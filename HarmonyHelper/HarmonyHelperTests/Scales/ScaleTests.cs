using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Eric.Morrison.Harmony.Tests
{
    [TestClass()]
    public class ScaleTests
    {
        [TestMethod()]
        public void ScaleTest()
        {
            Assert.Fail();
        }

        [TestMethod()]
        public void todoTest()
        {

            "TODO"
#if false

            Chord = Bb7;
            foreach (var scale in scaleThesaurus)
            {
                if (scale.Contains(chord)) //implies implicit IEnumerable<NoteName> conversion operator!!!
                {
                    Debug.Write(scale.Name.ToString());
                    Debug.Write(scale.ToString());
                    Debug.Write(" contains: ");
                    Debug.Write(chord.Name.ToString());
                }
            }

#endif
            Assert.Fail();
        }


    }//class
}//ns